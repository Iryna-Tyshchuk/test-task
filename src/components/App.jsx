import { Box } from './Box.styled';
import { Button } from './Button/Button';
import { UserCard } from './UserCard/UserCard';

export const App = () => {
  return (
    <Box>
      <UserCard />
    </Box>
  );
};
